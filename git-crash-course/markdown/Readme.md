## Markdown Examples

    -headings
    -unordered lists
    -text formatting
    -code
    -tables
    -links
    -images
    -autolists
    -lists

## Unordered Lists

We can create unordered lists in markdown using hyphens
